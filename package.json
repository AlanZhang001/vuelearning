{
  "name": "vueleanring",
  "version": "1.0.0",
  "description": "学习vue",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "vue2"
  ],
  "author": "alanzhang",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "http-server": "^0.9.0"
  },
  "scripts":{
    "prestart":"npm install",
    "start":"http-server ./demo -a localhost -p 8000 -c-1 -o"
  }
}
